<%= form_for @race, html: { multipart: true } do |f| %>
  <% if @race.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@race.errors.count, "error") %> prohibited this race from being saved:</h2>

      <ul>
      <% @race.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>

  <div class="field">
    <%= f.label :locality %><br>
    <input id="user_input_autocomplete_address" placeholder="Your city..."><br>
    <%= f.hidden_field :locality, readonly: true, id: "locality", datageo: "test" %>
  </div>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, size: "80x8" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

  <% end %>